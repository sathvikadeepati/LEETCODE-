class Solution {
    //recursion approach -o(2^n) o(n)
    // if(n==0) return 0;
    // if(n==1) return 1;
    // return fib(n-1)+fib(n-2);

    //------------ memorization -o(n) ,space -o(n)
    // private int solve(int n,int[] dp){
    //    if(n==0) return 0;
    //     if(n==1) return 1;
    //     if(dp[n]!=-1) return dp[n];
    //     return dp[n]=solve(n-1,dp)+solve(n-2,dp); 
    // }
    // public int fib(int n) {
    //     int[] dp=new int[n+1];
    //     Arrays.fill(dp,-1);
    //     return solve(n,dp);
    // }
    private int solve(int n){
        int[] dp=new int[n+1];
        if(n==0 || n==1) return n;
        dp[0]=0;
        dp[1]=1;
        for(int i=2;i<=n;i++){
            dp[i]=dp[i-1]+dp[i-2];
        }
        return dp[n]; 
    }
    public int fib(int n) {
        return solve(n);
    }
}
